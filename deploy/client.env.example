# Copy to ~/.hermes-bridge/.env on every machine running an agent (surrogate VPS,
# home machine, a friend's/brother's machine, ...).
HERMES_SERVER_URL=https://bridge.example.com
HERMES_AGENT_NAME=surrogate1
HERMES_AGENT_TOKEN=replace-with-the-token-printed-by-register-agent
